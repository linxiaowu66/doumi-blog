mode: 'local'
malagu:
  webpack:
    config:
      devtool: 'source-map'
  logger:
    winstonConfig:
      level: 'info'
    dailyRotateConfig:
      frequency: '24h'
      filename: 'doum-blog-%DATE%.log'
      dirname: './log'
