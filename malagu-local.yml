mode: 'local'
malagu:
  webpack:
    config:
      devtool: 'source-map'
  logger:
    winstonConfig:
      level: 'info'
    dailyRotateConfig:
      frequency: '1d'
      filename: 'doumi-blog-%DATE%.log'
      dirname: './log'
